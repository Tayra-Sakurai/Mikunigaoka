<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mikunigaoka.ItemEditAddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikunigaoka"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Mikunigaoka.Controls"
    xmlns:models="using:Sakaishi.Models"
    xmlns:viewmodels="using:Sakaishi.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:ItemViewModel}"
    DataContext="{x:Bind viewModel, Mode=OneWay}"
    mc:Ignorable="d">

    <Grid RowSpacing="8"
          ColumnSpacing="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Uid="ItemAddOrEditMsg"
                   Style="{StaticResource BodyTextBlockStyle}"/>
        <local:ItemEntryView Grid.Row="1"/>
        <CommandBar DefaultLabelPosition="Right"
                    Grid.Row="3">
            <AppBarButton Icon="Save"
                          x:Uid="SaveButtonItem"
                          Command="{x:Bind viewModel.SaveCommand}"/>
            <AppBarButton Icon="Delete"
                          x:Uid="DeleteItemButton"
                          Command="{x:Bind viewModel.DeleteCommand}"/>
        </CommandBar>
    </Grid>
</Page>

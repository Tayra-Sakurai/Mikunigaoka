<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mikunigaoka.PaymentMethodEditionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikunigaoka"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Padding="12,16"
        RowSpacing="8"
        ColumnSpacing="4"
        Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Style="{StaticResource BodyTextBlockStyle}"
            TextWrapping="WrapWholeWords"
            x:Uid="PaymentMethodEditDesc"/>

        <local:PaymentMethodView
            Grid.Row="1"
            DataContext="{x:Bind viewModel, Mode=TwoWay}"/>

        <CommandBar
            Grid.Row="3"
            DefaultLabelPosition="Right">
            <AppBarButton
                Icon="Save"
                x:Uid="SavePaymentMethodButton"
                Command="{x:Bind viewModel.UpdateCommand}"/>
            <AppBarButton
                Icon="Delete"
                x:Uid="DeletePaymentMethodButton"
                Command="{x:Bind viewModel.DeleteCommand}"/>
        </CommandBar>
    </Grid>
</Page>

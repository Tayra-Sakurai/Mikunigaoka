<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mikunigaoka.PaymentMethodsViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikunigaoka"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Sakaishi.Models"
    xmlns:converters="using:Sakaishi.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:CurrencyConverter x:Key="SuperConverter"/>
    </Page.Resources>

    <Grid
        RowSpacing="8"
        ColumnSpacing="4"
        Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
        Padding="12,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock>
            <Run Text="{x:Bind viewModel.PaymentMethods.Count}"/>
            <Run x:Uid="FoundNumberLabel"/>
        </TextBlock>

        <ListView
            ItemsSource="{x:Bind viewModel.PaymentMethods}"
            x:Name="SuperListView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PaymentMethod">
                    <StackPanel Orientation="Vertical">
                        <TextBlock
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="{x:Bind Name}"/>
                        <TextBlock
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind Balance, Converter={StaticResource SuperConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

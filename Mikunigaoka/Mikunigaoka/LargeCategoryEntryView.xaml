<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Mikunigaoka.LargeCategoryEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikunigaoka"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Mikunigaoka.Controls"
    xmlns:viewmodels="using:Sakaishi.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:LargeCategoryViewModel}"
    mc:Ignorable="d">

    <Grid Padding="0"
          RowSpacing="8"
          HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Uid="LargeCategorydesc"
                   Style="{StaticResource BodyTextBlockStyle}"
                   TextWrapping="WrapWholeWords"/>
        
        <controls:ValidationTextBox x:Uid="LargeCategoryName"
                                    Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    PropertyName="Name"
                                    Grid.Row="1"/>
    </Grid>
</UserControl>
